{
  "name": "engineering-planning-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Shared engineering planning board — fifteen registers driven by the team's own Excel workbook",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
